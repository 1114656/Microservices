spring:
  application:
    name: diary-diary-service
  profiles:
    active: ${SPRING_PROFILES_ACTIVE:local}
  config:
    import:
      - optional:nacos:diary-common.yaml?group=${NACOS_GROUP:DIARY_GROUP}
      - optional:nacos:${spring.application.name}.yaml?group=${NACOS_GROUP:DIARY_GROUP}
  main:
    allow-circular-references: true
  datasource:
    dynamic:
      primary: master
      datasource:
        master:
          url: jdbc:mysql://${MYSQL_HOST:127.0.0.1}:${MYSQL_PORT:3306}/${MYSQL_DATABASE:diary_diary}?useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true&nullCatalogMeansCurrent=true&rewriteBatchedStatements=true
          username: ${MYSQL_USERNAME:diary_diary}
          password: ${MYSQL_PASSWORD:diary_diary_pwd}
  data:
    redis:
      host: ${REDIS_HOST:127.0.0.1}
      port: ${REDIS_PORT:6379}
      database: ${REDIS_DATABASE:3}
      password: ${REDIS_PASSWORD:}
      repositories:
        enabled: false
  cloud:
    nacos:
      server-addr: ${NACOS_ADDR:127.0.0.1:8848}
      discovery:
        server-addr: ${spring.cloud.nacos.server-addr}
        namespace: ${NACOS_NAMESPACE:}
        group: ${NACOS_GROUP:DIARY_GROUP}
      config:
        server-addr: ${spring.cloud.nacos.server-addr}
        namespace: ${NACOS_NAMESPACE:}
        group: ${NACOS_GROUP:DIARY_GROUP}
        file-extension: yaml
    sentinel:
      transport:
        dashboard: ${SENTINEL_DASHBOARD:127.0.0.1:8080}

server:
  port: ${SERVER_PORT:48083}

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics

mybatis-plus:
  type-aliases-package: ${diary.info.base-package}.module.*.dal.dataobject

diary:
  info:
    version: 1.0.0
    base-package: com.xiaoyang.diary
